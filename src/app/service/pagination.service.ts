import {Page} from '../model/page';

export class PaginationService {

  public pagination<T>(page: number, size: number, p: Page<T>): Page<T> {
    const totalElement = p.totalElement;

    return null;
  }


}
